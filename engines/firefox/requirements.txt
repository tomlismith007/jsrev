# Firefox engine (camoufox-reverse-mcp) runtime dependencies.
# Install:  pip install -r requirements.txt
# Then:     camoufox fetch        # download the Camoufox browser binary
mcp>=1.29,<2
camoufox[geoip]>=0.4.0
playwright>=1.40.0
esprima>=4.0.1
